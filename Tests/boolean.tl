sys_println("--Boolean Tests");
a = true;
b = true;
c = false;

//assert((a || (b && c)) == true);
//assert((a + (b * c)) == true);
//assert((!a + (b * c)) == false);

//println("--Assertions Complete");

scope();
